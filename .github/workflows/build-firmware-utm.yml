name: Patch fNOS Image for UTM
on:
  workflow_dispatch: # 手动触发

permissions:
  contents: write # 赋予 Release 写入权限

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y fdisk xz-utils grep sed tar
          echo "✅ 基础依赖库安装完成"

      - name: Get Latest Release Info and Download
        id: get_info
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          REPO="ophub/fnnas"
          TAG="fnnas_base_image"
          
          echo "🔍 正在搜索最新附件..."
          # 按数字版本号排序，确保获取全场最高版本
          LATEST_ASSET=$(gh release view "$TAG" -R "$REPO" --json assets \
            -q '.assets[] | select(.name | endswith(".img.xz")) | .name' | \
            sed -E 's/.*_([0-9]+)\.img\.xz/\1 &/' | \
            sort -n | \
            tail -n 1 | \
            cut -d' ' -f2-)
          
          if [ -z "$LATEST_ASSET" ]; then
            echo "❌ 错误：未找到 .img.xz 附件"
            exit 1
          fi
          
          echo "🎯 锁定最新镜像: $LATEST_ASSET"
          VERSION=$(echo "$LATEST_ASSET" | grep -oP '(?<=_)\d+(?=\.img\.xz)' || echo "unknown")
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "SOURCE_FILE=$LATEST_ASSET" >> $GITHUB_ENV
          
          echo "📥 开始下载: $LATEST_ASSET"
          gh release download "$TAG" -R "$REPO" -p "$LATEST_ASSET" --clobber

      - name: Decompress and Patch Image
        run: |
          echo "📦 正在解压镜像..."
          unxz -v -c "${{ env.SOURCE_FILE }}" > source.img
          rm "${{ env.SOURCE_FILE }}"

          sudo losetup -D
          LOOP_DEV=$(sudo losetup -Pf --show source.img)
          mkdir -p mnt_p1 mnt_p2

          # 1. 提取内核组件
          sudo mount "${LOOP_DEV}p1" mnt_p1
          echo "📂 正在探测原始内核文件..."
          KERNEL_PATH=$(sudo find mnt_p1 -name "vmlinuz-*" ! -name "*.old" | sort -V | tail -n 1)
          INITRD_PATH=$(sudo find mnt_p1 -name "initrd.img-*" ! -name "*.old" | sort -V | tail -n 1)
          
          RAW_KERNEL=$(basename "$KERNEL_PATH")
          RAW_INITRD=$(basename "$INITRD_PATH")
          cp "$KERNEL_PATH" ./"$RAW_KERNEL"
          cp "$INITRD_PATH" ./"$RAW_INITRD"
          
          echo "RAW_KERNEL=$RAW_KERNEL" >> $GITHUB_ENV
          echo "RAW_INITRD=$RAW_INITRD" >> $GITHUB_ENV
          sudo umount mnt_p1

          # 2. 修正系统配置并提取极致精简 Rootfs
          sudo mount "${LOOP_DEV}p2" mnt_p2
          
          # 修改 fstab：禁用 boot 挂载，并将根目录设为 /dev/vdb
          sudo sed -i 's/^.*\/boot/#&/' mnt_p2/etc/fstab
          sudo sed -i 's/^\/dev\/mmcblk0p2/\/dev\/vdb/' mnt_p2/etc/fstab
          
          # 移除物理开发板冲突服务
          sudo rm -f mnt_p2/etc/systemd/system/multi-user.target.wants/trim_miniscreen.service
          sudo rm -f mnt_p2/etc/systemd/system/multi-user.target.wants/trim_wayland.service
          
          echo "💾 正在提取纯净系统分区为 rootfs.img..."
          # 将修改好的 P2 导出为 rootfs.img
          sudo dd if="${LOOP_DEV}p2" of=rootfs.img bs=1M status=none
          
          sudo umount mnt_p2
          sudo losetup -D
          rm source.img
          echo "✅ 极致精简 rootfs.img 提取完成"

      - name: Create README for UTM
        run: |
          cat << EOF > README.md
          # fNOS UTM 虚拟机使用说明 (v${{ env.VERSION }})

          ## 🚀 UTM 配置关键点
          1. **驱动器**: 导入镜像 \`rootfs.img\`，接口选 **VirtIO**。
          2. **高级**:
             - 勾选 **"使用本地内核/initrd"**。
             - **External Kernel**: 选择 \`${{ env.RAW_KERNEL }}\`。
             - **Initrd**: 选择 \`${{ env.RAW_INITRD }}\`。
          3. **启动参数 (Arguments)**:
             请在 QEMU 参数 最后一栏填入以下内容（包含引号）：
             \`\`\`text
             -append
             "root=/dev/vdb rw console=tty0 console=ttyAMA0 earlycon"
             \`\`\`
          EOF

      - name: Final Packaging (Tar + XZ)
        run: |
          # 创建一个以版本号命名的目录
          PKG_DIR="fn_utm_${{ env.VERSION }}"
          mkdir -p "$PKG_DIR"
          
          # 移动所有产物到目录中，镜像文件现在叫 rootfs.img
          mv rootfs.img "$PKG_DIR/"
          mv "${{ env.RAW_KERNEL }}" "$PKG_DIR/"
          mv "${{ env.RAW_INITRD }}" "$PKG_DIR/"
          mv README.md "$PKG_DIR/"
          
          # 打包并压缩
          echo "🗜️ 正在将所有文件打包压缩为 ${PKG_DIR}.tar.xz ..."
          tar -cvf - "$PKG_DIR" | xz -z -T0 -v > "${PKG_DIR}.tar.xz"
          
          echo "FINAL_PKG_NAME=${PKG_DIR}.tar.xz" >> $GITHUB_ENV

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: fnnas_utm
          name: fnnas_utm
          body: |
            fnOS UTM虚拟机ARM版
          files: ${{ env.FINAL_PKG_NAME }}
          overwrite_files: true
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
